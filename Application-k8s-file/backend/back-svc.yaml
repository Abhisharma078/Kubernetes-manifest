apiVersion: apps/v1
kind: Service
metadata:
  name: back-svc
  labels:
    app: back
spec: 
  type: ClusterIP
  selector:
    app: back
  ports:
  - name: back-port
    protocol: TCP
    port: 8080
    targetPort: 8080
  