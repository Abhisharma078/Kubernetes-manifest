apiVersion: v1
kind: Service
metadata:
  name: fron-svc
  labels:
    app: fron
spec:
  type: ClusterIP
  selector:
    app: fron
  ports:
  - name: fron-port
    port: 80
    protocol: TCP
    targetPort: 80
    