apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  labels:
    app: nginx
  namespace: nginx
spec:
  type: ClusterIP
  selector: 
    app: nginx
  ports:
   - name: nginx-port
     protocol: TCP
     port: 80
     targetPort: 80
