apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
  labels: 
    app: pvc
spec:
  resources:
    requests:
      storage: 5Gi
  accessModes:
  - ReadWriteOnce
  storageClassName: standard
