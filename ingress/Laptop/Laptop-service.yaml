apiVersion: v1
kind: Service
metadata:
  name: laptop-svc
  labels:
    app: laptop
spec:
  type: ClusterIP
  selector:
    app: laptop
  ports:
  - name: laptop-port
    protocol: TCP
    port: 80
    targetPort: 80