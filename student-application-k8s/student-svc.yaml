apiVersion: v1
kind: Service
metadata:
  name: student-svc
  labels:
    app: student
spec:
  type: NodePort
  selector:
    app: student
  ports:
   - name: student-port
     port: 8080
     targetport: 8080
     nodePort: 30001
     protocol: TCP